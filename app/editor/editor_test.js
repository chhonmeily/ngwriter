'use strict';

describe('ngWriter.editor module', function() {

  beforeEach(module('ngWriter.editor'));

  describe('editor controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var editorCtrl = $controller('editorController');
      expect(editorCtrl).toBeDefined();
    }));

  });
});